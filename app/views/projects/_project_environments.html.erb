<table class="table table-striped">
  <tr>
    <th>Environment Name</th>
    <th>Actions</th>    
  </tr>

<% @graphs.each do |graph| %>
  <tr>
    <td><%= graph.name %></td>
    <td><%= link_to 'Edit', edit_project_graph_path(@project,graph) %>&nbsp;&nbsp; | &nbsp;&nbsp;
    <%= link_to 'Destroy', project_graph_path(@project,graph), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%#= link_to 'New Environment', new_graph_path(:project => @project.id) %>
<%= link_to 'New Environment', new_project_graph_path(@project) %>&nbsp;&nbsp; | &nbsp;&nbsp;
<%= link_to 'Projects', projects_path %>