<div class = "pull-right">
  <ul class="nav nav-pills">
    <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="<%= projects_path %>"><%= @project.name.present? ? @project.name : ''%> &nbsp;&nbsp;:</a>
    </li>
    <li class="dropdown" style="margin-left:-12%;">
      <a class="dropdown-toggle" data-toggle="dropdown" href="<%= project_path(@project) %>"><%= @graph.name.present? ? @graph.name : ''%></a>
    </li>
  </ul> 
</div>
