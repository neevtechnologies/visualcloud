<div class = "pull-right">
  <ul class="nav nav-pills">
    <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="#">Project &nbsp;:&nbsp;<%= @project.name.present? ? @project.name.capitalize : ''%></a>
    </li>
    <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="#">Environment &nbsp;:&nbsp;<%= @graph.name.present? ? @graph.name.capitalize : ''%></a>
    </li>
  </ul> 
</div>
