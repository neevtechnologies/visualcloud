<%if flash.now[:error].blank? %>
  window.location = '<%= edit_project_environment_path(@environment.project_id,@environment.id)%>';
<%else%>
  hideLoading();
  showFlashMessages($('#container'), '','<%= escape_javascript flash_messages %>');
<%end%>
